# Core Django
Django>=5.1,<5.2

# Django Extensions
django-simple-history>=3.4.0  # Model history tracking
django-tailwind>=3.6.0         # Tailwind CSS integration
django-browser-reload>=1.12.0  # Development auto-reload
Pillow>=10.0.0                 # Image processing (for ImageField)

# Testing
pytest>=7.4.0
pytest-django>=4.5.0
pytest-cov>=4.1.0
factory-boy>=3.3.0
Faker>=19.0.0

# Production Security (optional but recommended)
# Uncomment these for production deployment:
# django-axes>=6.0.0           # Login attempt tracking & rate limiting
# django-ratelimit>=4.1.0      # General rate limiting decorator
# django-csp>=3.8              # Content-Security-Policy headers
# gunicorn>=21.2.0             # Production WSGI server
# psycopg2-binary>=2.9.9       # PostgreSQL adapter
# redis>=5.0.0                 # Redis client for caching/sessions
# django-redis>=5.4.0          # Django Redis cache backend
# sentry-sdk>=1.40.0           # Error tracking and monitoring
